<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackgroud"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/titlebar_height"
        android:background="@color/titlebar_bg">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="登录"
            android:textColor="#ffffff"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/login_tv_regist"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="right"
            android:gravity="center_vertical"
            android:paddingLeft="@dimen/titlebar_paddingLeft"
            android:paddingRight="15dp"
            android:text="注册"
            android:textColor="#ffffff"
            android:textSize="16sp" />
    </FrameLayout>

    <android.support.design.widget.TabLayout
        android:id="@+id/login_tab"
        android:layout_width="match_parent"
        android:layout_height="@dimen/home_tab_height"
        android:background="#ffffff"
        app:tabIndicatorColor="@color/titlebar_bg"
        app:tabSelectedTextColor="@color/titlebar_bg"
        app:tabTextAppearance="@style/tabTextApp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/colorSplitLine" />

    <android.support.v4.view.ViewPager
        android:id="@+id/login_vp"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>